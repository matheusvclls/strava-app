repos:
-   repo: local
    hooks:
      - id: requirements
        name: requirements
        entry: batch -c 'pip freeze > requirements.txt'
        language: system
        pass_filenames: false
        stages: [commit]